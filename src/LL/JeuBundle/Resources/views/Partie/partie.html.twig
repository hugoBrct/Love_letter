{% extends '::squelette.html.twig' %}

{% block jeu %}
    <h1>Partie {{ table.id }} !</h1>
    <h2>Etat : {{ table.etat }}</h2>

    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            <button id="lancerPartie" type="button" class="btn btn-primary">Lancer partie !</button>
        </div>
    </div>

    <ul>
        {% for pioche in listCarte %}
            <li>{{ pioche.carte.nom }} : {{ pioche.carte.point }}</li>
            <img src="../../../../web/skin_cartes/{{ pioche.carte.img }}">
        {% endfor %}
    </ul>

    <div class="row">
        {% for joueur in joueur %}
            <div class="col-md-3 ">
                <h4>Joueur : {{ joueur.email }} Score : {{ joueur.score }} </h4>
            </div>
        {% endfor %}
    </div>


    <script type="text/javascript">
        var interval = 30000;
        setTimeout(function(){
            window.location.reload(false);
        }, interval);


        $("#lancerPartie").on('click', function(){
            function lancerPartie(){

            }
        });

    </script>



{% endblock %}